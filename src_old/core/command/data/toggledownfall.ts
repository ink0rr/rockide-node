import { CommandInfo } from "../types";

const toggledownfall: CommandInfo = {
  command: "toggledownfall",
  documentation: "Toggles the weather.",
  overloads: [
    {
      params: [],
    },
  ],
};
export default toggledownfall;
