import { CommandInfo } from "../types";

const list: CommandInfo = {
  command: "list",
  documentation: "Lists players on the server.",
  overloads: [
    {
      params: [],
    },
  ],
};
export default list;
