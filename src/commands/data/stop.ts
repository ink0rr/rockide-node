import { CommandInfo } from "../types";

const stop: CommandInfo = {
  command: "stop",
  documentation: "Stops the server.",
  overloads: [
    {
      params: [],
    },
  ],
};
export default stop;
